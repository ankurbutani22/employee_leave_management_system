# Employee Frontend

Scaffolded React + Vite + Tailwind employee frontend for leave management.

Run locally:

```powershell
cd employee
npm install
npm run dev
```

Routes:
- `/auth` - sign in / sign up
- `/home` - homepage
- `/leave-request` - leave request form
- `/leave-status` - my leave status
- `/my-profile` - profile
